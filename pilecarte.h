#ifndef PILECARTE_H
#define PILECARTE_H
/*
class PileCarte {
 protected:
  // Collection de cartes dans la pile
  vector<Carte> cards;
  // Indicateur de pile visible par les joueurs
  bool visible;

 public:
  // Constructeur
  CardStack(bool visible);
  // Méthode permettant d'ajouter une carte à la pile
  void addCard(Carte card);
  // Méthode permettant de retirer une carte de la pile
  Carte removeCard();
  // Accesseur en lecture pour l'indicateur de visibilité de la pile
  bool isVisible();
  // Accesseur en écriture pour l'indicateur de visibilité de la pile
  void setVisible(bool visible);
};*/
#endif